name: Contract Checks

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  contracts:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: make check
